/*
** EPITECH PROJECT, 2019
** PSU_2019_malloc
** File description:
** TODO: CHANGE DESCRIPTION.
*/

#include <stdio.h>
#include <string.h>
#include <assert.h>
//#include "malloc.h"


int main(int ac, char **av) {
    char *a = malloc(10);
    assert(a != NULL);
    //a = realloc(a, 6540);
    //char *b = malloc(20);
    //assert(b != NULL);
    //char *c = malloc(5);
    //assert(c != NULL);
    //my_free(c);
    //c = malloc(5);
    ////strcpy(c, "hello world\n");
    ////write(2, c, strlen(c));
    //char *d = malloc(3990);
    //assert(d != NULL);
    //char *e = malloc(6);
    //assert(e != NULL);
    //char *f = malloc(100);
    //assert(f != NULL);
    //my_free(a);
    return (0);
}
